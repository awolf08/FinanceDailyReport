name: Retired finance publisher
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  moved:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo '::error::Report generation moved to awolf08/reports. Run daily-report.yml there.'
          exit 1
